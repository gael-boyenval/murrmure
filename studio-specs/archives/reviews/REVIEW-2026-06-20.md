# Studio-v2 review — 6-agent audit vs inputs/studio

**Date:** 2026-06-20  
**Scope:** Full specs + build specs in `research/studio-v2/` against `inputs/studio/`  
**Agents:** 6 × gpt-5.3-codex-xhigh (CR, FS, TR, XS, CL, cross-cutting)

---

## Executive summary

| Area | Verdict |
|------|---------|
| **Overall handoff** | Buildable, not self-sufficient — dev can implement mechanics but will hit contradictions without fixes |
| **Capability runtime** | Close — core sound; missing rollback, grant-change, harness fixtures |
| **Feature-spec** | Directionally strong — governance/semver/dedup inconsistencies |
| **Triggers** | No-go for TR1 sign-off — schema drift, wrong J07 mapping, thin fixtures |
| **Cross-space** | XS-min OK — not Théo-grade; policy + federation under-specified |
| **Cloud shell** | CL0 framed — CL1 not audit-ready |

**Cross-cutting:** 27/40 journeys referenced; 13 unmapped. Spec vs build vs fixture contradictions on dedup keys, `wake_label` vs `tool`, denial codes. No single E2E chain fixture.

---

## Cross-cutting (agent 6)

### Strengths
- `HANDOFF.md` is a clear entry point
- Spec + bridge + fixture triads exist per phase-2 layer
- Phase 1 / phase 2 split is mostly clear in `build/README.md`

### Blockers
1. **13 journeys unmapped** in traceability (J08, J18, c02-J02–J08, c02-J10, c02-J15, c02-J16, c02-J20)
2. **FS→TR→XS data path conflict:** trigger forwards `body_ref`; cross-space says summary-only — bypasses minimum disclosure
3. **Dedup:** `spec_key` only (triggers spec) vs `spec_key + version` (fixtures/build)
4. **Trigger action naming:** `wake_label` / `tool` / `wake_mcp_agent` drift across docs
5. **Acceptance under-covers DoD** in build/01–05

### Top 10 fixes (priority)
1. 40-journey ledger with status per journey
2. Normalize IDs to `c01-Jxx` / `c02-Jxx`
3. Unify dedup to `(spec_key, version)`
4. Canonical trigger action schema (`wake_label`; legacy alias once)
5. Remove `body_ref` from default wake payload; require `query_ask spec_summary@1`
6. Expand `build/acceptance.md` for all layer DoD claims
7. One E2E chain fixture (CR→FS→TR→XS→CL)
8. Split README “full stack order” vs “phase 2 now”
9. HANDOFF “assumptions” section (what’s external)
10. Doc precedence rule in `technical-index.md`: spec > bridge > build > fixtures

---

## Capability runtime (agent 1)

**Verdict:** Close but not sufficient for journey-complete implementation.

| Gap | Detail |
|-----|--------|
| J11 rollback | No rollback golden fixture |
| J17 drift | No denied-invoke observability wire spec |
| J10 | Fixture tests minor bump only, not breaking gate |
| J16 | Missing “did you mean space?” hint |
| J13 | No “agent behind contract version” UX |

**Fix:** Add error contract with hints; rollback + grant-change + harness fixtures; expand DoD for J10/J11/J17.

---

## Feature-spec (agent 2)

**Verdict:** Not audit-tight — governance/semver/dedup inconsistent.

| Gap | Detail |
|-----|--------|
| J02/J20 | No decision rule: `spec.published` vs `work.ready` vs review |
| c02-J14 | Only happy-path `spec_summary@1`; no allowlist denial fixture |
| c02-J18 | No redaction rule for title/summary free text |
| FSM | Missing `in_review → draft` (request changes) |
| MCP semver | `transition_spec` in fixtures but not in 1.0.0 contract |

**Fix:** Add decision table in spec; fix contract transitions; complete HTTP bridge; negative-path DoD.

---

## Triggers (agent 3)

**Verdict:** No-go for TR1 — schema drift, wrong journey mapping.

| Gap | Detail |
|-----|--------|
| J02 template | Not fully normative in triggers/spec.md |
| J06 | Dedup reason enums inconsistent (`business_key_match` vs `duplicate_fingerprint`) |
| J07 mapping | **Wrong:** traceability maps integration_failure to token-leak J07 |
| J15 | Wake pending / relay semantics not specified |
| Security | `trigger:register`, allow-list, registration audit under-specified |

**Fix:** Freeze canonical trigger schema; full J02 template; fix J07 mapping; add failure + catalog + relay fixtures.

---

## Cross-space (agent 4)

**Verdict:** XS-min pass; not Théo-grade.

| Gap | Detail |
|-----|--------|
| c02-J14 | No `context_fetch@1` normative contract |
| c02-J12 | No open-schema rejection / finance minimization |
| c02-J09 | Isolation weaker than journey expects |
| c02-J13 | `TARGET_SPACE_UNREACHABLE` only — no queue/drain |
| Codes | `POLICY_DENIED` vs `QUERY_POLICY_DENIED` |

**Fix:** Normative `query_policy`; Théo query types; federation local_only; 4+ new fixtures.

---

## Cloud shell (agent 5)

**Verdict:** CL0 mostly framed; CL1 not audit-ready.

| Gap | Detail |
|-----|--------|
| J15 | Relay health contract undefined |
| c02-J11 | Single-space fixture vs full onboarding |
| J12 | Generic export vs SOC2 evidence classes |
| J07 | No CSRF / session hardening spec |
| Parity | Config shell routes/SSE/denial not mapped for cloud |

**Fix:** Auth hardening in cloud/spec; parity matrix vs config; CL1 fixtures (CI, relay, audit, abuse).

---

## Recommended next step

Before giving `studio-v2/` to dev for phase 2:

1. **P0 — contract alignment** (dedup, wake_label, denial codes, body_ref in wake payload) — 1 pass across triggers + cross-space + feature-spec
2. **P1 — fix J07 mapping bug** in journey-traceability
3. **P1 — expand acceptance.md** + 8–10 missing fixtures
4. **P2 — 40-journey ledger** + E2E chain fixture

Without P0, dev will implement conflicting schemas.

---

## Applied fixes (2026-06-20)

- Unified trigger schema: `mcp_wake`, `wake_label`, `key_jsonpaths`, `duplicate_business_key`
- Removed `body_ref` from default wake payload; agent uses `query_ask` / `spec_summary@1`
- Fixed traceability: Hub ADR-14 for integration_failure (not c01-J07); c01-J07 → cloud auth
- `QUERY_POLICY_DENIED` canonical; added `query_policy`, `context_fetch@1` to cross-space spec
- Feature-spec: decision table, `request_changes` transition, redaction rules
- CR: error envelope with hints, grant-change catalog refresh
- Cloud: session cookie hardening, `/configure/hub` health DTO
- Config: cross-space worker grant template note
- New fixtures: rollback-live-mount, query-policy-denied, phase2-full-chain, updated trigger fixtures
- 40-journey ledger in journey-traceability.md
- Doc precedence in technical-index + HANDOFF assumptions
