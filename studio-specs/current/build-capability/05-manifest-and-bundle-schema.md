# Manifest and bundle schema (v1)

**Status:** normative (2026-06-23)  
**Supersedes:** P5 `mount_export`-only manifest for new flows  
**FDK package:** `@murrmure/flow-dev-kit` `FlowManifestSchema`

---

## Manifest schemaVersion `"1"` (`flow.manifest.json`)

```typescript
export const FlowManifestSchema = z.object({
  schemaVersion: z.literal("1"),
  id: z.string().regex(/^[a-z][a-z0-9-]{1,62}$/),     // flow_id
  version: z.string(),                                  // semver
  routes_prefix: z.string().regex(/^\/api\/[a-z0-9-]+$/),
  ui: z.object({
    entry: z.string(),                                  // relative → bundled ui/entry.js
    canvas_route: z.string(),                           // e.g. /spaces/:spaceId/sessions/:sessionKey
    shell_html: z.string().optional(),                  // default: ui/shell.html (iframe host page)
    assets: z.array(z.string()).optional(),
  }),
  server: z.object({
    mount_module: z.string(),                           // relative → server/mount.mjs
  }),
  mcp_tools_by_version: z.record(z.string(), z.array(z.string())), // semver key → tool names
  config_schema: z.string().optional(),                 // contract/config.schema.json
  tests: z.object({ contract: z.string().optional() }).optional(),
  // contract_ref_id: NOT in author manifest — hub assigns at ingest (ARCH-03)
});
```

### Migration from P5

| P5 field | v1 field |
|----------|----------|
| `mount_export` | `server.mount_module` (path in bundle, not npm package name) |
| `mcp_tools: string[]` | `mcp_tools_by_version[version]` |
| `contract_ref_id` | **removed from author manifest** — hub assigns |
| `capability.manifest.json` | `flow.manifest.json` |

---

## Stage tree (after `mrmr flow build`)

Staged under `~/.murrmure/flows/{flow_id}/{version}/{stage}/`:

```
{stage}/
  manifest.json              # resolved paths (bundled-relative)
  contract/
    contract.json
    config.schema.json       # if manifest.config_schema set
    mcp-tools.json           # tool schemas + route map
  ui/
    shell.html               # iframe bootstrap; loads entry.js
    entry.js                 # ESM; exports mount()
    assets/…
  server/
    mount.mjs                # ESM; exports mountRoutes()
  source/                    # author source snapshot (not executed at runtime)
    flow.manifest.json
    package.json
    contract/
    ui/src/
    server/
    tests/
  bundle.tar.zst             # runtime-only tar (ui/, server/, contract/, manifest.json)
  source.tar.zst             # source/ tree only
  bundle.digest              # sha256 of bundle.tar.zst bytes
  source.digest              # sha256 of source.tar.zst bytes
  build.meta.json
  .flow-push-state.json      # written on push (CLI), excluded from digests
```

---

## Deterministic digests

| Rule | Value |
|------|-------|
| Algorithm | SHA-256 over canonical tar.zst bytes |
| Canonicalization | Sorted file paths; normalized mtimes stripped; UTF-8 JSON keys sorted |
| Prefix | `sha256:` + lowercase hex |
| `bundle.digest` | Hash of `bundle.tar.zst` |
| `source.digest` | Hash of `source.tar.zst` |
| Excluded from hash | `.flow-push-state.json`, `build.meta.json`, `*.digest`, raw dirs when tar present |

Hub **recomputes** both digests on ingest; rejects if client `bundle.digest` or `source.digest` mismatch (ARCH-04).

Push **rejected** if `source.tar.zst` missing (`SOURCE_BUNDLE_MISSING`).

---

## build.meta.json

```json
{
  "flow_id": "review-loop-lite",
  "version": "1.0.0",
  "bundle_digest": "sha256:…",
  "source_digest": "sha256:…",
  "source_path": "/Users/dev/my-workflows/review-loop-lite",
  "built_at": "2026-06-20T12:00:00Z",
  "cli_version": "0.1.0",
  "dev_kit_version": "0.1.0",
  "ui_framework": "esbuild-react"
}
```

Configure install detail displays `source_path`, `bundle_digest`, `source_digest`, `built_at` (UX-05).

In v1, `ui_framework` is constrained to the React build profile (`"esbuild-react"`) for scaffolds generated by `mrmr flow init`.

---

## contract/mcp-tools.json

```json
{
  "tools": {
    "create_review_session": {
      "http": { "method": "POST", "path": "/sessions" },
      "input_schema": { "type": "object", "properties": { … } }
    }
  }
}
```

Lens A: every name in `mcp_tools_by_version[manifest.version]` must exist in `tools`.

Hub MCP rebuild uses these schemas — see [07-mcp-tool-model-and-catalog-rebuild.md](./07-mcp-tool-model-and-catalog-rebuild.md).

---

## contract.json

Hub contract schema v2 — see [../hub/architecture.md](../hub/architecture.md) Part 6. Author maintains in user project; immutable once blob stored.

---

## Minor vs major semver (MCP tools)

| Bump | `mcp_tools_by_version` |
|------|------------------------|
| Patch | Same tool list |
| Minor | Add tools; optional new keys in schemas |
| Major | May remove/rename tools; new `contract_ref_id` major from hub |

Runtime honors `mcp_tools_by_version[live semver]` on catalog rebuild.

---

## Validation (offline)

CLI `validateFlowRoot` / FDK schema blocking:

- Manifest schema
- Bundle tree completeness (post-build)
- Source tree required paths (post-build)
- Contract graph reachability
- Tool ↔ route map consistency
- `routes_prefix` / `canvas_route` syntax

Warnings (Lens B): missing tests, config schema drift vs live install.

---

## Related

- [06-install-push-apply-http-contract.md](./06-install-push-apply-http-contract.md)
- [01-local-layout.md](./01-local-layout.md)
