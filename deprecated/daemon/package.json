{
  "name": "@studio/daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "studio": "./bin/studio"
  },
  "scripts": {
    "dev": "node --import tsx src/cli/index.ts daemon start",
    "start": "node --import tsx src/cli/index.ts daemon start",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@studio/review-contracts": "file:../review-contracts",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
