# Private hub environment. Copy to .env.local and chmod 600.
# Hub CLI startup loads this file once; restart the hub after edits.
# Existing process environment keys win. Values are never logged.

MURRMURE_ENV_FIXTURE=
