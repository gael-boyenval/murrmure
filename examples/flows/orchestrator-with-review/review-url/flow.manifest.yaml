apiVersion: murrmure.flow/v1
name: review-url
description: Generic URL review sub-flow (flow_call entry only)
start:
  flow_call: true
  manual: false
steps:
  - id: review
    invoke:
      space: "{{origin_space}}"
      action: overnight_research
      params:
        url: "{{input.url}}"
