version: 1
actions:
  submit_brief_output:
    executor: shell
    command: echo '{"format":"markdown","body":"# Daily brief"}'
    cwd: "{{space_root}}"
    delivery: fail_fast
    timeout_ms: 30000
  mcp_wake:
    executor: shell
    command: echo '{"wake":"handle_brief_requested"}'
    cwd: "{{space_root}}"
    delivery: fail_fast
    timeout_ms: 30000
