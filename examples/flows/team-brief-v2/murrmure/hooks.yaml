version: 1
hooks:
  brief_published_wake:
    on:
      event:
        type: brief.published
    do:
      - invoke:
          action: mcp_wake
          params:
            wake_label: handle_brief_published
