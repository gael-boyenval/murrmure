version: 1
actions:
  team_brief_open:
    executor: shell
    command: echo '{"brief_id":"brf_demo"}'
    cwd: "{{space_root}}"
    delivery: fail_fast
    timeout_ms: 30000
  mcp_wake:
    executor: shell
    command: echo '{"wake":"handle_brief_published"}'
    cwd: "{{space_root}}"
    delivery: fail_fast
    timeout_ms: 30000
