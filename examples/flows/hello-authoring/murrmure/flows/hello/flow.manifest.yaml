# Role: flows tutorial — minimal invoke-only indexed flow.
apiVersion: murrmure.flow/v1
name: hello
description: Hello invoke — single shell_spawn step

triggers:
  manual: true

start:
  manual: true

steps:
  - id: hello
    invoke:
      space: "{{origin_space}}"
      action: hello_hello
