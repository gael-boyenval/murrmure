{
  "type": "object",
  "properties": {
    "default_preview_url": {
      "type": "string",
      "description": "Preview URL used when create_review_session omits a url."
    },
    "production_gate_enabled": {
      "type": "boolean",
      "default": true,
      "description": "Require a human gate before request_production converges to production_approved."
    }
  }
}
