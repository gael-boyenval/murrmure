// Handler dispatch on open steps is introduced in Task 05/06. Task 03 removes
// role-based dispatch discrimination and does not implement handlers; the
// tutorial flow for Part 2 has no handlers. These runtime dispatch tests are
// owned by Tasks 05/06 and are reactivated there.
import { describe, test } from "vitest";

describe.skip("flow-engine/handler-dispatch (Task 05/06)", () => {
  test("handler dispatch on open steps is implemented in Task 05/06", () => {});
});
