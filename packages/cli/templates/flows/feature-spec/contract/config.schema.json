{
  "type": "object",
  "properties": {
    "skip_review": {
      "type": "boolean",
      "default": false,
      "description": "When true, an author may publish_direct from draft without a reviewer."
    },
    "required_approver_role": {
      "type": "string",
      "default": "spec_approver",
      "description": "Role required to satisfy approve/publish gates."
    },
    "default_target_repo": {
      "type": "string",
      "description": "Optional repository attached to newly opened specs."
    }
  }
}
