# Role: step handler merged into .mrmr/space/handlers.yaml on scaffold.
version: 1
handlers:
  - id: "{{FLOW_ID}}-hello"
    on: step.opened::{{FLOW_ID}}.hello
    type: shell_spawn
    complete: explicit
    command: node .mrmr/space/scripts/{{FLOW_ID}}-hello.mjs
