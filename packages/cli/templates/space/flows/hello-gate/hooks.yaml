# Role: optional event handlers — edit and run `mrmr space apply` to index.
version: 1
hooks: {}
# Example:
# hooks:
#   on_spec_published:
#     on:
#       event:
#         type: mrmr.spec.published
#     type: shell_spawn
#     complete: auto
#     command: mrmr flow run {{FLOW_ID}}
