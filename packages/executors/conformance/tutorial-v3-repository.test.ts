import { describe, test } from "vitest";

describe("Tutorial v3 repository conformance", () => {
  test.skip("Task 10 — clean repository archives and commits owned paths", () => {});
  test.skip("Task 10 — dirty repository fails before mutation", () => {});
  test.skip("Task 10 — broad staging and .mrmr/dev commits are impossible", () => {});
});

