version: 1

handlers:
  - id: hello
    contract_keys: [demo.hello]
    on: step.opened
    type: shell_spawn
    complete: auto
    command: echo '{"status":"ok"}'
    cwd: "{{space_root}}"
    delivery: fail_fast
    timeout_ms: 30000
